name: Build Windows App

on:
  workflow_dispatch:
  push:
    branches:
      - main
  release:
    types: [created]

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 45
    env:
      KIVY_NO_ARGS: "1"
      KIVY_WINDOW: "mock"
      KIVY_GL_BACKEND: "mock"
      KIVY_AUDIO: "mock"
      KIVY_NO_CONSOLELOG: "1"
      KIVY_DOC: "1"
      KIVY_UNITTEST: "1"
      KIVY_DPI: "96"
      PYI_LOG_LEVEL: "INFO"
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-build.txt

      - name: Environment diagnostics
        run: |
          python --version
          pip --version
          python -m PyInstaller --version
          echo "KIVY_WINDOW=$env:KIVY_WINDOW"
          echo "KIVY_GL_BACKEND=$env:KIVY_GL_BACKEND"
          echo "KIVY_DOC=$env:KIVY_DOC"
          echo "KIVY_UNITTEST=$env:KIVY_UNITTEST"
          echo "KIVY_DPI=$env:KIVY_DPI"
          pip show kivy
          pip show kivymd
          pip show reportlab
          pip show pyinstaller-hooks-contrib

      - name: Build executable (PyInstaller)
        run: |
          python -m PyInstaller --noconfirm --clean --windowed --log-level INFO --name JPresupuesto --hidden-import kivy_deps.angle --hidden-import kivy_deps.glew --hidden-import kivy_deps.sdl2 --exclude-module kivy.tests app.py > pyinstaller.log 2>&1
          $exitCode = $LASTEXITCODE
          if ($exitCode -ne 0) {
            Write-Host "PyInstaller fallo. Ultimas lineas de pyinstaller.log:"
            Get-Content pyinstaller.log -Tail 200
            exit $exitCode
          }

      - name: Show build log tail (on failure)
        if: failure()
        run: |
          if (Test-Path "pyinstaller.log") {
            Write-Host "=== pyinstaller.log (ultimas 300 lineas) ==="
            Get-Content pyinstaller.log -Tail 300
          } else {
            Write-Host "pyinstaller.log no existe en el workspace."
          }

      - name: Validate build output
        run: |
          if (!(Test-Path "dist/JPresupuesto")) {
            Write-Error "No existe dist/JPresupuesto luego del build."
            exit 1
          }

      - name: Package build output
        run: |
          Compress-Archive -Path dist/JPresupuesto/* -DestinationPath dist/JPresupuesto-windows.zip -Force

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-windows
          path: |
            pyinstaller.log
          if-no-files-found: ignore

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: JPresupuesto-windows
          path: dist/JPresupuesto-windows.zip

      - name: Upload release asset
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/JPresupuesto-windows.zip
